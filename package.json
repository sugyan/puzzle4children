{
  "name": "puzzle4children",
  "private": true,
  "version": "0.1.0",
  "description": "小学校低学年むけ 知育パズル自動生成ツール（A4印刷・鉛筆で解く）",
  "author": "sugyan",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "esbuild scripts/smoke.ts --bundle --format=esm --platform=node --outfile=node_modules/.cache/smoke.mjs && node node_modules/.cache/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
